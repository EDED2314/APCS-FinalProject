package project;

//might not use this

public class CourtClients extends Court{

    public CourtClients(int radius, CustomPoint center, int playerNum) {
        super(radius, center, playerNum);
    }

    @Override
    public void update(boolean[] keys){
        //in this case how we want to update it is that once everyone is in sync we update
        //special update method because
        return;
    }


}
